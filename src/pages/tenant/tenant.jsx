import React from 'react';
import RegisterDomain from '../../domains/Register/views/Register';

const RegisterPage = () => {
    return (
        <RegisterDomain />
    );
};

export default RegisterPage;
